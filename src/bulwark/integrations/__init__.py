"""Bulwark integrations with LLM providers.

Each integration is a separate module with its own SDK dependency.
Import directly from the submodule:

    from bulwark.integrations.anthropic import make_analyze_fn, make_execute_fn, make_pipeline
"""
