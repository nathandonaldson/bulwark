[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bulwark-ai"
version = "0.1.0"
description = "Architectural defense against prompt injection. Because detection is an arms race."
readme = "README.md"
license = "MIT"
requires-python = ">=3.9"
keywords = ["ai", "security", "prompt-injection", "llm", "agent"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.39.0"]
cli = ["click>=8.0"]
testing = ["garak>=0.9,<1.0"]
all = ["anthropic>=0.39.0", "click>=8.0", "garak>=0.9,<1.0"]

[project.scripts]
bulwark = "bulwark.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
